// SPDX-License-Identifier: AGPL-3.0-or-later

// Open-edition stub for PivtIcon. Returns nothing — pivt branding is
// enterprise-only. Type matches a permissive SVG icon shape so accidental
// renders in open code (cases / notebooks consumers, all stubbed elsewhere)
// don't fail at runtime.

import type { SVGProps } from 'react';

export function PivtIcon(_props?: SVGProps<SVGSVGElement>) {
  return null;
}
